{
  "name": "tzvet-chrome-extension-template",
  "version": "0.1.0",
  "description": "Minimal Chrome Extension template (Manifest V3) — popup, options, background service worker, content script, and build script.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node ./scripts/generate-icons.js",
    "preview": "vite preview",
    "clean": "node -e \"const fs=require('fs');if(fs.existsSync('dist')){fs.rmSync('dist',{recursive:true,force:true});console.log('dist removed')}\"",
    "lint": "eslint . --ext .js",
    "test": "node ./scripts/check-build.js"
  },
  "keywords": [
    "chrome-extension",
    "manifest-v3",
    "template"
  ],
  "license": "MIT",
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.3.0",
    "@tailwindcss/postcss": "^4.1.18",
    "autoprefixer": "^10.4.23",
    "daisyui": "^5.5.14",
    "eslint": "^8.0.0",
    "http-server": "^14.1.1",
    "jest": "^29.0.0",
    "postcss": "^8.5.6",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.1.18",
    "vite": "^5.0.0"
  }
}
